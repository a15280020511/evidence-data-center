# 情报中心境外出口 IP 与境外处理补充规则

版本：`1.0`

生效基线：`2026-08-03`

适用范围：Cloudflare、GitHub Actions、Hugging Face、境外 VPS、境外代理、外国 IP、全球边缘节点及其他境外自动化执行环境。

## 1. 基本原则

1. 使用外国 IP、境外服务器或境外工具，不改变数据源的授权边界，也不排除中国法律、平台合同和中国法院管辖。
2. 外国 IP 只能是技术出口，不得作为隐藏身份、规避限流、绕过地域限制、逃避封禁或替代正式授权的手段。
3. 是否合规以数据是否真实公开、是否获得授权、是否绕过技术措施、采集规模、数据属性、用途和损害后果判断，不以出口 IP 所在国家判断。
4. 数据直接写入境外服务器、境外对象存储或允许境外主体访问，均按境外处理和潜在数据跨境场景审查。

## 2. 可使用固定外国 IP 的条件

只有同时满足以下条件，才允许通过固定外国出口 IP 自动采集：

- 数据无需登录、付费、验证码、项目账号或临时账号即可访问；
- 来源为官方开放 API、官方公开下载或面向不特定公众的公开网页；
- 网站条款、接口许可和合同未禁止境外访问、境外存储、自动化访问或当前用途；
- 不包含个人信息、敏感个人信息、国家秘密、工作秘密、核心数据或疑似重要数据；
- 不访问后台、隐藏接口、内部参数、未公开 API 或受限数据；
- 使用固定、可审计的出口，不使用代理池或 IP 轮换；
- 单域名单并发、低频、增量采集，不影响目标服务；
- 采集任务票据明确记录出口国家或地区、云服务商、固定 IP 或节点标识、存储地点和数据分类；
- 出现拒绝信号时立即停止。

## 3. 外国 IP 强制禁止行为

禁止以下行为：

- 使用境外代理池、住宅代理、动态 IP、Tor、VPN 轮换或多区域边缘节点规避限流和封禁；
- 在收到 `401`、`403`、`429`、CAPTCHA、WAF、地域限制或账号异常提示后切换国家、节点、IP 或账号继续采集；
- 利用外国 IP 绕过仅限中国大陆、仅限合同客户、仅限指定 IP 白名单或仅限人工查询的访问条件；
- 伪造来源地区、设备指纹、请求头、Cookie、签名、Token 或用户行为；
- 通过境外节点调用未公开 API、破解加密接口、遍历参数或扩大账号授权范围；
- 将中经数据、企查查、天眼查等商业数据库网页抓取后写入境外仓库，以规避其官方接口、费用、导出或存储限制；
- 将个人信息、重要数据、完整商业数据库或合同限制数据直接写入 Cloudflare、GitHub、Hugging Face 或境外 VPS；
- 将外国 IP 作为减少被发现、隐藏真实采集主体或逃避责任的设计目标。

## 4. 政府网站规则

1. 外国 IP 访问政府公开网页本身不当然违法，但不产生额外采集授权。
2. 优先级固定为：`官方 API > 官方开放下载 > 官方订阅 > 公开网页低频增量采集`。
3. 政府网站对境外 IP 返回拒绝、验证码、WAF 或地域限制时，必须停止，不得切换代理或节点绕过。
4. 不得全站镜像、重复全量扫描、目录爆破、参数遍历或采集后台、测试系统和内部接口。
5. 政务公开材料中包含个人联系方式、身份证件、家庭地址等内容时，必须在写入境外环境前删除或不可逆匿名化。
6. 多源汇聚后可能形成重要数据、敏感空间数据、关键设施分布或个人画像的，必须重新分类，不得因单条数据公开而自动放行。

## 5. 中经数据及其他商业数据库

1. 仅允许使用官方 API、SFTP、授权文件交付或书面合同约定的建库方式。
2. 使用外国 IP、Cloudflare 或境外云调用前，必须确认合同允许：
   - 境外来源 IP；
   - 境外执行环境；
   - 境外缓存或存储；
   - 机器调用和批量处理；
   - 当前字段、调用量和用途；
   - 内部分析、派生结果和再分发方式。
3. 合同未明确允许境外存储时，原始数据必须留在批准的境内环境；境外环境最多接收经许可、最小化、匿名化后的分析输入或聚合结果。
4. 任何网页登录、共享账号、自动导出、抓取页面、破解次数或地域限制的方案均为 `BLOCKED`。

## 6. 境外处理与数据跨境

1. 数据直接从中国网站写入境外服务器，或存储在境内但可被境外主体远程访问，均应按潜在跨境处理进行识别和留痕。
2. 公开、非个人、非重要数据通常不因使用外国 IP 自动触发数据出境安全评估，但仍须遵守来源许可、知识产权、反不正当竞争、网络安全和合同义务。
3. 涉及个人信息时，必须评估合法性基础、最小必要、告知或同意、个人信息保护影响、境外接收方义务及适用的数据出境机制。
4. 涉及重要数据、核心数据、国家秘密、工作秘密或无法完成分类的数据，不得进入境外环境。

## 7. 强制票据字段

使用外国 IP 或境外工具的任务，票据必须增加：

```json
{
  "foreign_egress": true,
  "egress_provider": "provider-name",
  "egress_country_or_region": "country-or-region",
  "egress_mode": "fixed-ip|documented-edge-region",
  "ip_rotation": false,
  "geo_restriction_bypass": false,
  "processing_location": "actual-location",
  "storage_location": "actual-location",
  "foreign_remote_access": false,
  "source_terms_allow_foreign_access": true,
  "source_terms_allow_foreign_storage": true,
  "contains_personal_data": false,
  "contains_important_data": false,
  "manual_review_status": "APPROVED"
}
```

任一字段为未知、矛盾或无法证明时，任务状态必须为 `MANUAL_REVIEW` 或 `BLOCKED`。

## 8. 自动停止与证据保全

出现以下任一事件必须立即停止：

- `401`、`403`、`429`；
- CAPTCHA、WAF、地域限制、IP 封禁；
- 服务条款、许可、合同或开放范围发生变化；
- 返回未申报的个人信息、内部字段或疑似重要数据；
- 请求延迟、错误率或流量异常；
- 上游投诉、停止通知或律师函。

停止后不得自动更换国家、IP、代理、账号或备用域名。必须保存请求时间、固定出口、响应状态、来源条款快照、任务票据和停止原因，并经新的人工审批后方可恢复。

## 9. 生产判定

- 固定外国 IP + 官方开放接口 + 公开非个人非重要数据 + 条款允许 + 低频增量：`CONDITIONAL_ALLOW`。
- 固定外国 IP + 政府公开 HTML + 无拒绝信号 + 不境外保存原始个人数据：`MANUAL_REVIEW` 后可执行。
- 外国 IP + 商业数据库官方 API + 合同明确允许境外执行和存储：`MANUAL_REVIEW` 后可执行。
- 外国 IP + 商业数据库网页抓取：`BLOCKED`。
- 代理池或 IP 轮换 + 绕过限流、封禁、WAF、验证码或地域限制：`BLOCKED`。
- 境外环境处理个人信息、重要数据或合同限制数据且未完成相应合规程序：`BLOCKED`。
