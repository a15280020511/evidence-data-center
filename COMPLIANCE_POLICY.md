# 情报中心数据采集与使用合规守则

版本：`1.0`

生效基线：`2026-08-03`

适用仓库：`a15280020511/evidence-data-center`

正式名称：**情报中心（Intelligence Center）**

> 本守则是情报中心的强制运行边界，不是一般性建议。任何连接器、API、网页采集器、Cloudflare Worker、GitHub Actions、数据清洗任务、数据碰撞任务、知识图谱任务和结果发布任务均须遵守。

## 1. 最高原则

1. **授权优先**：能使用官方 API、开放数据下载、正式 MCP、SFTP 或书面授权的，不使用网页抓取。
2. **最小必要**：只采集完成具体任务必需的字段、范围、频率和时间段。
3. **只读低扰动**：默认只读、单域名单并发、增量更新，不得影响目标服务正常运行。
4. **不绕过控制**：不得绕过登录、验证码、WAF、签名、加密参数、下载限制、风控、访问频率限制或 IP 封禁。
5. **来源可证明**：每批数据必须可追溯到来源、访问时间、授权依据、采集方式和内容哈希。
6. **用途受约束**：取得数据的方式合法，不代表后续碰撞、画像、再分发、商业利用或境外存储当然合法。
7. **默认保护个人**：默认拒绝采集直接联系方式、身份号码、家庭地址、账户凭证、行踪轨迹及其他敏感个人信息。
8. **不以技术可行为合规依据**：能访问、能下载、能破解、能模拟用户，不等于获得法律或合同授权。

## 2. 数据源分级

### 2.1 绿色来源：可自动执行

满足全部条件时可进入自动化采集：

- 政府开放数据平台提供的官方 API、CSV、Excel、JSON、XML 或明确下载入口；
- 官方 API 或数据服务合同明确允许的字段和用途；
- 无需登录、未设置访问控制、未禁止自动化访问的政府公开页面；
- 数据不含直接个人标识、敏感个人信息、国家秘密、工作秘密或疑似重要数据；
- 不存在禁止缓存、禁止境外存储、禁止再分发等冲突性条款；
- 采集频率服从上游明确限制。

### 2.2 黄色来源：必须人工合规复核

出现任一情形即进入人工复核：

- 网站内容公开，但服务条款、Robots 规则或版权声明不明确；
- 需要普通注册账号、付费订阅、临时账号或项目账号；
- 数据包含法定代表人、股东、董监高、诉讼当事人等自然人信息；
- 需要批量采集、历史回溯、跨库碰撞、人物关系穿透或风险评分；
- 计划存储在 Cloudflare、GitHub、Hugging Face 或其他可能位于境外的平台；
- 数据供应商限制缓存期限、调用主体、用途、用户范围、导出或再分发；
- 单一公开数据与其他数据关联后可能形成敏感画像、精确关系网络或重大影响判断。

未经复核通过，黄色任务不得执行。

### 2.3 红色来源或行为：强制阻断

以下行为无条件禁止：

- 使用盗用、购买、共享、泄露、冒用或超出授权范围的账号、Token、Cookie、证书；
- 使用临时账号获取项目范围之外的数据，或在授权到期后继续访问；
- 破解或模拟签名算法、加密接口、验证码、设备指纹、登录态、CSRF、防重放机制；
- 使用代理池、IP 轮换、账号轮换、请求伪装等方式规避封禁、限流或风控；
- 在收到 `401`、`403`、`429`、验证码、WAF 挑战、封禁提示或停止通知后继续采集；
- 逆向未公开接口、遍历隐藏参数、利用漏洞、越权读取后台或内部数据；
- 绕过下载条数、页面条数、时间窗口或导出次数限制；
- 抓取企查查、天眼查、中经数据等商业数据库网页，以替代其官方 API、MCP、SFTP 或授权产品；
- 采集身份证号、手机号、私人邮箱、家庭住址、金融账户、精确位置、未成年人信息等非必要个人数据；
- 获取、保存、分析、转移国家秘密、工作秘密、核心数据或未经评估的重要数据；
- 将合同限制数据、个人信息、重要数据或完整商业数据库上传到公开仓库或未经批准的境外平台；
- 复制实质性数据集合建立替代性产品、镜像站、转售库或对外批量查询服务；
- 开发、销售、提供或维护专门用于突破目标系统安全措施的数据采集工具。

## 3. 政府网站采集规则

1. 首选顺序固定为：
   `官方 API > 官方开放数据下载 > 官方订阅/数据目录 > 公开网页低频增量采集`。
2. 仅采集向不特定公众公开且无需绕过访问控制的内容。
3. 不得访问行政后台、内部系统、测试系统、未公开接口或通过参数遍历扩展访问范围。
4. 单个域名默认最大并发为 `1`；公开 HTML 默认平均间隔不短于 `3 秒`。上游规定更严格时服从上游。
5. 仅做增量更新；禁止无必要的全站重复镜像。
6. 遇到 `429` 应立即停止并按 `Retry-After` 执行；无明确恢复时间时，本任务不得自动重试。
7. 遇到 `401`、`403`、验证码、WAF 或封禁，视为授权边界，不得通过技术手段绕过。
8. 政府公开信息中夹带的个人联系方式、身份证件、家庭地址等字段必须在入库前删除或不可逆脱敏。
9. 对多个政府数据源进行碰撞时，必须重新评估汇聚结果是否形成敏感信息、重要数据或对个人权益产生重大影响。
10. 法律法规、政策文件、统计数据应保存发布机关、文号、发布日期、有效状态、原始链接和抓取时间。

## 4. 商业数据库规则

### 4.1 天眼查

- 仅允许使用官方开放平台和正式授权接口；
- 严格限制在合同许可的字段、调用量、缓存期限、用途、展示和存储区域内；
- 返回结果应默认删除直接联系方式和不必要的个人身份字段；
- 禁止网页抓取、登录态自动化导出和规避次数限制。

### 4.2 企查查

- 仅允许官方 API、MCP、CLI、批量服务或书面授权的数据交付；
- 在正式接入前，必须完成合同许可矩阵；
- 未取得正式授权时，连接器状态必须为 `BLOCKED`，不得用网页爬虫替代。

### 4.3 中经数据及其他付费数据库

- 仅允许官方 API、SFTP、授权文件交付或合同约定的数据建库方式；
- 禁止共享账号批量下载、破解导出限制、复制登录后页面或对外重售原始数据；
- 原始数据不得进入公开 GitHub 仓库或公开 Hugging Face Dataset；
- 合同条款与本守则冲突时，执行更严格的一方。

## 5. 个人信息与企业人员数据

1. 企业名称、统一社会信用代码与自然人信息必须分层存储。
2. 法定代表人、股东、董监高姓名属于自然人信息，只有在任务具有明确目的且确有必要时方可处理。
3. 已公开个人信息也只能在合理范围内使用；不得因其公开而无限制画像、聚合、传播或永久保存。
4. 默认禁止存储：
   - 身份证号、护照号；
   - 手机号、私人邮箱；
   - 家庭住址、精确位置；
   - 金融账户、健康、生物识别、行踪轨迹；
   - 未成年人信息；
   - 登录凭证、Cookie、Token、验证码。
5. 关系图谱不得将推断事实冒充已证实事实。推断边必须标注来源、算法、置信度和生成时间。
6. 不得仅依据自动化碰撞结果对自然人作出重大不利判断、公开指控或高风险标签。
7. 对外输出默认以企业级、区域级或统计级结果为主；个人级输出必须单独审批。

## 6. 跨境与存储规则

Cloudflare、GitHub、Hugging Face 等平台可能涉及境外基础设施或境外访问。下列数据默认不得上传：

- 直接个人标识和敏感个人信息；
- 商业数据库原始数据包或完整镜像；
- 合同禁止境外存储、境外访问或再分发的数据；
- 国家秘密、工作秘密、核心数据、重要数据或无法排除上述属性的数据；
- 登录后、付费后、临时授权或内部接口取得的数据。

允许上传的数据应满足：公开、非个人、非敏感、来源合法、许可允许、已最小化，并保留来源与授权证据。

GitHub 仓库只保存代码、配置、数据目录、哈希、脱敏样例和审计回执；不得将生产原始数据作为代码附件长期保存。

## 7. 每个采集任务的强制票据

任务执行前必须生成不可变票据，至少包含：

```json
{
  "task_id": "unique-id",
  "source_owner": "数据发布或经营主体",
  "source_url": "固定来源",
  "access_method": "official-api|official-download|public-html",
  "authorization_basis": "许可、合同、开放声明或公开依据",
  "terms_snapshot_sha256": "条款或许可快照哈希",
  "purpose": "具体任务目的",
  "fields": ["实际所需字段"],
  "contains_personal_data": false,
  "data_classification": "public",
  "collection_scope": "时间、地域、对象和数量边界",
  "rate_limit": "上游限制或本中心保守限制",
  "storage_location": "实际保存位置",
  "cross_border": false,
  "retention_policy": "删除或复核条件",
  "redistribution": false,
  "review_status": "AUTO_GREEN|MANUAL_APPROVED"
}
```

缺少任一关键字段、声明与实际数据不一致或无法证明授权依据时，任务必须失败关闭。

## 8. 自动停止条件

采集器出现以下任一事件必须立即停止，不得自动切换代理、账号或备用路径：

- `401`、`403`、`429`；
- CAPTCHA、WAF、设备验证、登录验证；
- 上游明确拒绝、投诉、律师函、下架或停止通知；
- 响应结构出现未申报的个人信息、密钥、内部字段或疑似重要数据；
- 请求延迟、错误率或流量异常，可能影响目标服务；
- 接口条款、Robots 规则、授权状态发生变化；
- 采集数量、费用、时间或字段超出票据边界；
- 数据来源无法验证或内容完整性校验失败。

停止后只能由新的合规票据恢复，不得原任务静默重启。

## 9. 数据使用、碰撞与发布

1. 原始层、脱敏层、分析层必须逻辑隔离。
2. 数据碰撞只能使用票据中批准的字段和目的。
3. 新增数据源、扩大目的、增加字段、延长保存期或变更存储地点，视为新任务，必须重新审批。
4. 发布结果必须标明来源、时间、置信度、推断与事实的区分。
5. 不得发布可反向识别个人的微观数据集。
6. 不得向外部用户提供绕过供应商许可的批量查询、下载或镜像能力。
7. 收到有效删除、纠错、投诉或权利主张后，应冻结相关结果并启动复核。

## 10. 审计证据

每次执行至少保存：

- 任务票据与 SHA256；
- 代码提交 SHA、工作流 Run ID；
- 数据源和条款快照哈希；
- 实际请求次数、响应状态码、退避和停止记录；
- 原始内容哈希、转换脚本版本、脱敏记录；
- 数据字段、数量、时间范围、存储位置；
- 输出 Artifact 和保留期限；
- 人工审批人、审批时间和审批结论（如适用）。

日志不得记录 API Key、Cookie、Token、密码、身份证号、手机号或完整个人信息。

## 11. 事件响应

发现越权、误采、个人信息泄露、合同违反、异常下载或目标系统受影响时：

1. 立即停止任务并禁用相关连接器；
2. 隔离数据，禁止继续分析和发布；
3. 吊销或轮换相关密钥、账号和 Token；
4. 保全日志、票据、提交 SHA 和 Artifact，禁止篡改；
5. 确认受影响数据、人员、系统和存储位置；
6. 删除无合法依据取得的数据及其派生副本；
7. 按法律、合同和平台要求履行通知、整改或报告义务；
8. 根因修复和独立复核通过前，不得恢复生产。

## 12. 责任与执行

- 任何人不得以“数据公开”“仅内部研究”“技术测试”“没有获利”作为绕过本守则的理由。
- 任务发起者负责证明授权和用途；连接器负责技术阻断；情报中心负责审计留痕。
- 违反红色规则的任务不得通过人工紧急开关放行。
- 故意虚假申报数据来源、个人信息属性、存储地点或授权状态，按重大安全事件处理。
- 本守则必须进入代码审查、连接器验收和生产发布门；仅有文档而无机器阻断，不视为落实。

## 13. 生产准入检查表

只有全部回答“是”才可执行：

- [ ] 来源主体、URL 和数据权利边界清楚；
- [ ] 已优先采用官方 API 或正式数据交付；
- [ ] 不需要绕过任何技术或账户限制；
- [ ] 数据字段与具体目的直接相关且最小化；
- [ ] 个人信息、重要数据、保密信息已经排除或依法审批；
- [ ] 调用频率和总量不会影响目标服务；
- [ ] 合同允许当前用途、缓存、存储区域和输出方式；
- [ ] Cloudflare、GitHub、Hugging Face 的存储风险已评估；
- [ ] 已建立来源、条款、请求、数据和输出的证据链；
- [ ] 已配置 `401/403/429/CAPTCHA/WAF` 自动停止；
- [ ] 已配置删除、纠错、投诉和事故响应流程；
- [ ] 结果不会成为目标平台数据产品的实质性替代。

任一项为“否”或“不确定”，状态必须为 `BLOCKED` 或 `MANUAL_REVIEW`。

## 14. 法律基线

本守则主要依据下列现行制度建立：

- 《中华人民共和国刑法》关于非法获取计算机信息系统数据、非法控制计算机信息系统及相关单位犯罪的规定；
- 最高人民法院、最高人民检察院关于办理危害计算机信息系统安全刑事案件的司法解释；
- 《中华人民共和国网络安全法》；
- 《中华人民共和国数据安全法》；
- 《中华人民共和国个人信息保护法》；
- 《网络数据安全管理条例》；
- 《中华人民共和国反不正当竞争法》（2025年修订）；
- 《中华人民共和国著作权法》；
- 数据供应商合同、用户协议、开放许可和技术访问规则。

法律、司法解释、监管规则或供应商合同发生变化时，应立即重新评估相关连接器；在评估完成前，受影响连接器降级为 `BLOCKED`。
