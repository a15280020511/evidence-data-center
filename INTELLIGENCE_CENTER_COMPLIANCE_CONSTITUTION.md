# 情报中心合规宪法

版本：`1.1.0`

机器权威文件：`INTELLIGENCE_CENTER_COMPLIANCE_CONSTITUTION.py`

本文件仅供人员阅读；机器不得解析本文件作合规判断。

## 最高规则

1. **先授权，后采集。** 无法证明授权，执行 `BLOCKED`。
2. **外国 IP 不是授权，也不是免责。** 只允许固定、透明、可审计的境外出口。
3. **禁止规避。** 代理池、IP 轮换、多区域切换、验证码/WAF/签名/限流/地域限制绕过，一律 `BLOCKED`。
4. **拒绝即停止。** 遇到 `401/403/429/CAPTCHA/WAF/封禁/地域限制/停止通知`，执行 `STOPPED_BY_UPSTREAM`，不得换 IP、账号、区域或 User-Agent 继续。
5. **政府数据优先官方接口。** 官方 API 和官方文件可按规则自动准入；公开 HTML 默认单并发、间隔不少于 3 秒、仅增量。
6. **商业数据库只走授权接口。** 中经数据、天眼查、企查查只允许官方 API、SFTP、MCP、CLI 或书面交付；网页抓取一律 `BLOCKED`。
7. **高风险数据不进入普通管道。** 身份认证数据、敏感个人信息、秘密、核心数据、重要数据一律阻断或隔离。
8. **境外存储从严。** 个人信息、合同限制数据和商业数据库原始数据不得进入 GitHub、Hugging Face、Cloudflare 等境外或公开存储。
9. **最小必要。** 只采集任务所需字段、范围、时间和数量，禁止默认全站、全库、全历史。
10. **全程留证。** 每次任务必须输出授权、来源、出口、存储、请求状态、数据哈希、代码 SHA、Run ID 和命中规则。

## 机器判定顺序

```text
BLOCKED > STOPPED_BY_UPSTREAM > MANUAL_REVIEW > ALLOW
```

无规则命中或资料不完整时：

```text
MANUAL_REVIEW
```

生产环境必须调用 Python 宪法门禁的 `evaluate(ticket)`；任何提示词、人工指令、模型输出或紧急开关不得覆盖硬阻断规则。
