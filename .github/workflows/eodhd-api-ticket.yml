name: Managed EODHD API Center

on:
  issues:
    types: [opened, reopened]

permissions:
  contents: read
  issues: write
  actions: read

concurrency:
  group: api-eodhd-ticket-${{ github.event.issue.number }}
  cancel-in-progress: false

jobs:
  execute-eodhd-api:
    if: >-
      github.actor == github.repository_owner &&
      startsWith(github.event.issue.title, '[api-eodhd]')
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    env:
      GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      ISSUE_NUMBER: ${{ github.event.issue.number }}
      EODHD_API_TOKEN: ${{ secrets.EODHD_API_TOKEN }}
    steps:
      - name: Checkout pinned source
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          persist-credentials: false

      - name: Set up isolated Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: api-center/eodhd/requirements.txt

      - name: Install pinned dependencies
        run: |
          python -m pip install --disable-pip-version-check --no-input \
            -r api-center/eodhd/requirements.txt
          python -m pip check

      - name: Compile managed provider control plane
        run: python -m py_compile api-center/eodhd/eodhd_task.py

      - name: Parse and authorize EODHD ticket
        id: prepare
        continue-on-error: true
        run: |
          python api-center/eodhd/eodhd_task.py prepare \
            --event-path "$GITHUB_EVENT_PATH" \
            --output-dir eodhd-artifacts

      - name: Comment accepted ticket
        if: steps.prepare.outputs.accepted == 'true'
        run: |
          python api-center/eodhd/eodhd_task.py render \
            --output-dir eodhd-artifacts --phase accepted > eodhd-comment.md
          gh api --method POST "repos/${GITHUB_REPOSITORY}/issues/${ISSUE_NUMBER}/comments" \
            -F body=@eodhd-comment.md

      - name: Comment rejected ticket
        if: steps.prepare.outputs.accepted != 'true'
        run: |
          python api-center/eodhd/eodhd_task.py render \
            --output-dir eodhd-artifacts --phase rejected > eodhd-comment.md
          gh api --method POST "repos/${GITHUB_REPOSITORY}/issues/${ISSUE_NUMBER}/comments" \
            -F body=@eodhd-comment.md

      - name: Execute bounded read-only EODHD operation
        id: execute
        if: steps.prepare.outputs.accepted == 'true'
        continue-on-error: true
        run: |
          python api-center/eodhd/eodhd_task.py execute \
            --ticket eodhd-artifacts/ticket.json \
            --output-dir eodhd-artifacts

      - name: Upload EODHD evidence
        id: upload
        if: always()
        continue-on-error: true
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: eodhd-api-ticket-${{ github.event.issue.number }}-${{ github.run_id }}
          path: eodhd-artifacts/
          if-no-files-found: error
          retention-days: 30

      - name: Publish verified result or structured failure
        if: always() && steps.prepare.outputs.accepted == 'true'
        env:
          ARTIFACT_URL: ${{ steps.upload.outputs.artifact-url }}
        run: |
          python api-center/eodhd/eodhd_task.py render \
            --output-dir eodhd-artifacts \
            --phase completed \
            --artifact-url "$ARTIFACT_URL" > eodhd-comment.md
          gh api --method POST "repos/${GITHUB_REPOSITORY}/issues/${ISSUE_NUMBER}/comments" \
            -F body=@eodhd-comment.md

      - name: Mark rejected or failed execution
        if: >-
          always() &&
          (steps.prepare.outputs.accepted != 'true' ||
           steps.execute.outputs.status != 'API_EODHD_COMPLETED' ||
           steps.upload.outcome != 'success')
        run: |
          echo "EODHD API ticket did not complete successfully."
          exit 1
