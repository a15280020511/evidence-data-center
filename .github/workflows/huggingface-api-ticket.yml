name: Managed Hugging Face Hub Intelligence Center

on:
  issues:
    types: [opened, reopened]

permissions:
  contents: read
  issues: write
  actions: read

concurrency:
  group: intel-huggingface-global
  cancel-in-progress: false

jobs:
  execute-huggingface:
    if: >-
      github.actor == github.repository_owner &&
      startsWith(github.event.issue.title, '[intel-huggingface]')
    runs-on: ubuntu-24.04
    timeout-minutes: 12
    env:
      GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      ISSUE_NUMBER: ${{ github.event.issue.number }}
      HF_HUB_DISABLE_TELEMETRY: "1"
      HF_HUB_OFFLINE: "0"
    steps:
      - name: Checkout pinned source
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
        with:
          persist-credentials: false

      - name: Set up isolated Python
        uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: |
            api-center/requirements.txt
            api-center/huggingface/requirements.txt

      - name: Install pinned dependencies
        run: |
          set -euo pipefail
          python -m pip install --disable-pip-version-check --no-input \
            -r api-center/requirements.txt \
            -r api-center/huggingface/requirements.txt
          python -m pip check

      - name: Compile provider
        run: python -m py_compile api-center/managed_provider_runtime.py api-center/huggingface/huggingface_task.py

      - name: Parse and authorize Hugging Face ticket
        id: prepare
        continue-on-error: true
        run: python api-center/huggingface/huggingface_task.py prepare --event-path "$GITHUB_EVENT_PATH" --output-dir huggingface-artifacts

      - name: Comment accepted ticket
        if: steps.prepare.outputs.accepted == 'true'
        run: |
          python api-center/huggingface/huggingface_task.py render --output-dir huggingface-artifacts --phase accepted > provider-comment.md
          gh api --method POST "repos/${GITHUB_REPOSITORY}/issues/${ISSUE_NUMBER}/comments" -F body=@provider-comment.md

      - name: Comment rejected ticket
        if: steps.prepare.outputs.accepted != 'true'
        run: |
          python api-center/huggingface/huggingface_task.py render --output-dir huggingface-artifacts --phase rejected > provider-comment.md
          gh api --method POST "repos/${GITHUB_REPOSITORY}/issues/${ISSUE_NUMBER}/comments" -F body=@provider-comment.md

      - name: Execute one bounded public Hub method
        id: execute
        if: steps.prepare.outputs.accepted == 'true'
        continue-on-error: true
        env:
          HF_TOKEN: ""
          HUGGING_FACE_HUB_TOKEN: ""
        run: python api-center/huggingface/huggingface_task.py execute --ticket huggingface-artifacts/ticket.json --output-dir huggingface-artifacts

      - name: Upload evidence
        id: upload
        if: always()
        continue-on-error: true
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
        with:
          name: huggingface-ticket-${{ github.event.issue.number }}-${{ github.run_id }}
          path: huggingface-artifacts/
          if-no-files-found: error
          retention-days: 30

      - name: Publish verified result or structured failure
        if: always() && steps.prepare.outputs.accepted == 'true'
        env:
          ARTIFACT_URL: ${{ steps.upload.outputs.artifact-url }}
        run: |
          python api-center/huggingface/huggingface_task.py render --output-dir huggingface-artifacts --phase completed --artifact-url "$ARTIFACT_URL" > provider-comment.md
          gh api --method POST "repos/${GITHUB_REPOSITORY}/issues/${ISSUE_NUMBER}/comments" -F body=@provider-comment.md

      - name: Mark rejected or failed execution
        if: >-
          always() &&
          (steps.prepare.outputs.accepted != 'true' ||
           steps.execute.outputs.status != 'INTEL_HUGGINGFACE_COMPLETED' ||
           steps.upload.outcome != 'success')
        run: exit 1
