name: Professional Open APIs Provider Validate

on:
  pull_request:
    paths:
      - "api-center/professional-open-apis/**"
      - ".github/workflows/professional-open-apis-validate.yml"
      - ".github/workflows/professional-open-apis-ticket.yml"
  push:
    branches: [main]
    paths:
      - "api-center/professional-open-apis/**"
      - ".github/workflows/professional-open-apis-validate.yml"
      - ".github/workflows/professional-open-apis-ticket.yml"

permissions:
  contents: read

concurrency:
  group: professional-open-apis-validate-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    steps:
      - name: Checkout pinned source
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
        with:
          persist-credentials: false

      - name: Set up Python
        uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: |
            api-center/requirements.txt
            api-center/professional-open-apis/requirements.txt

      - name: Install pinned dependencies
        run: |
          python -m pip install --disable-pip-version-check --no-input \
            -r api-center/requirements.txt \
            -r api-center/professional-open-apis/requirements.txt
          python -m pip check

      - name: Compile
        run: python -m py_compile api-center/managed_provider_runtime.py api-center/professional-open-apis/professional_open_apis_task.py

      - name: Unit tests
        run: python -m unittest discover -s api-center/professional-open-apis/tests -p 'test_*.py' -v

      - name: Validate JSON documents
        run: |
          python - <<'PY'
          import json
          from pathlib import Path
          from jsonschema import Draft202012Validator
          root = Path('api-center/professional-open-apis')
          for path in sorted(root.glob('*.json')):
              json.loads(path.read_text(encoding='utf-8'))
          Draft202012Validator.check_schema(json.loads((root / 'ticket.schema.json').read_text(encoding='utf-8')))
          print('professional open APIs JSON and schema validation passed')
          PY
