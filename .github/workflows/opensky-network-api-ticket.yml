name: Managed OpenSky Network Intelligence Center

on:
  issues:
    types: [opened, reopened]

permissions:
  contents: read
  issues: write
  actions: read

concurrency:
  group: intel-opensky-global
  cancel-in-progress: false

jobs:
  execute-opensky:
    if: >-
      github.actor == github.repository_owner &&
      startsWith(github.event.issue.title, '[intel-opensky]')
    runs-on: ubuntu-24.04
    timeout-minutes: 12
    env:
      GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      ISSUE_NUMBER: ${{ github.event.issue.number }}
    steps:
      - name: Checkout pinned source
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
        with:
          persist-credentials: false

      - name: Set up isolated Python
        uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: |
            api-center/requirements.txt
            api-center/opensky-network/requirements.txt

      - name: Install pinned dependencies
        run: |
          python -m pip install --disable-pip-version-check --no-input \
            -r api-center/requirements.txt \
            -r api-center/opensky-network/requirements.txt
          python -m pip check

      - name: Compile provider
        run: python -m py_compile api-center/managed_provider_runtime.py api-center/opensky-network/opensky_network_task.py

      - name: Parse and authorize OpenSky ticket
        id: prepare
        continue-on-error: true
        run: python api-center/opensky-network/opensky_network_task.py prepare --event-path "$GITHUB_EVENT_PATH" --output-dir opensky-artifacts

      - name: Comment accepted ticket
        if: steps.prepare.outputs.accepted == 'true'
        run: |
          python api-center/opensky-network/opensky_network_task.py render --output-dir opensky-artifacts --phase accepted > opensky-comment.md
          gh api --method POST "repos/${GITHUB_REPOSITORY}/issues/${ISSUE_NUMBER}/comments" -F body=@opensky-comment.md

      - name: Comment rejected ticket
        if: steps.prepare.outputs.accepted != 'true'
        run: |
          python api-center/opensky-network/opensky_network_task.py render --output-dir opensky-artifacts --phase rejected > opensky-comment.md
          gh api --method POST "repos/${GITHUB_REPOSITORY}/issues/${ISSUE_NUMBER}/comments" -F body=@opensky-comment.md

      - name: Execute one bounded OpenSky request
        id: execute
        if: steps.prepare.outputs.accepted == 'true'
        continue-on-error: true
        env:
          OPEN_SKY_CLIENT_ID: ${{ vars.OPEN_SKY_CLIENT_ID }}
          OPEN_SKY_CLIENT_SECRET: ${{ secrets.OPEN_SKY_CLIENT_SECRET }}
        run: python api-center/opensky-network/opensky_network_task.py execute --ticket opensky-artifacts/ticket.json --output-dir opensky-artifacts

      - name: Upload OpenSky evidence
        id: upload
        if: always()
        continue-on-error: true
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
        with:
          name: opensky-ticket-${{ github.event.issue.number }}-${{ github.run_id }}
          path: opensky-artifacts/
          if-no-files-found: error
          retention-days: 30

      - name: Publish verified result or structured failure
        if: always() && steps.prepare.outputs.accepted == 'true'
        env:
          ARTIFACT_URL: ${{ steps.upload.outputs.artifact-url }}
        run: |
          python api-center/opensky-network/opensky_network_task.py render --output-dir opensky-artifacts --phase completed --artifact-url "$ARTIFACT_URL" > opensky-comment.md
          gh api --method POST "repos/${GITHUB_REPOSITORY}/issues/${ISSUE_NUMBER}/comments" -F body=@opensky-comment.md

      - name: Mark rejected or failed execution
        if: >-
          always() &&
          (steps.prepare.outputs.accepted != 'true' ||
           steps.execute.outputs.status != 'INTEL_OPENSKY_COMPLETED' ||
           steps.upload.outcome != 'success')
        run: exit 1
