name: Validate OpenStreetMap Provider

on:
  pull_request:
    paths:
      - "api-center/openstreetmap/**"
      - "api-center/build_catalog_market_search.py"
      - "api-center/api-catalog.json"
      - "api-center/api-catalog.md"
      - "api-center/tests/test_api_catalog.py"
      - ".github/workflows/openstreetmap-*.yml"
  push:
    branches: [main]
    paths:
      - "api-center/openstreetmap/**"
      - "api-center/build_catalog_market_search.py"
      - "api-center/api-catalog.json"
      - "api-center/api-catalog.md"
      - "api-center/tests/test_api_catalog.py"
      - ".github/workflows/openstreetmap-*.yml"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: validate-openstreetmap-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  deterministic-validation:
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
        with:
          persist-credentials: false
      - uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: |
            api-center/requirements.txt
            api-center/openstreetmap/requirements.txt
      - name: Install dependencies
        run: |
          python -m pip install --disable-pip-version-check --no-input \
            -r api-center/requirements.txt \
            -r api-center/openstreetmap/requirements.txt
          python -m pip check
      - name: Compile and run zero-network tests
        run: |
          python -m py_compile \
            api-center/managed_provider_runtime.py \
            api-center/openstreetmap/openstreetmap_task.py
          python api-center/openstreetmap/tests/test_openstreetmap_task.py
      - name: Verify deterministic aggregate catalog
        run: |
          python api-center/build_catalog_market_search.py \
            --json-output /tmp/api-catalog.json \
            --markdown-output /tmp/api-catalog.md
          cmp /tmp/api-catalog.json api-center/api-catalog.json
          cmp /tmp/api-catalog.md api-center/api-catalog.md
          python - <<'PY'
          import json
          from pathlib import Path
          catalog = json.loads(Path("api-center/api-catalog.json").read_text(encoding="utf-8"))
          providers = {row["provider_id"]: row for row in catalog["managed_providers"]}
          assert providers["openstreetmap"]["operation_count"] == 6
          assert providers["openstreetmap"]["secret_value_exposed"] is False
          assert catalog["managed_provider_count"] == 57
          assert catalog["managed_operation_count"] == 643
          assert catalog["direct_center_to_center_calls_allowed"] is False
          assert catalog["secret_values_exposed"] is False
          PY
          python api-center/tests/test_api_catalog.py
          git diff --check
