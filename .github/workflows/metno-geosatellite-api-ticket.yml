name: Managed MET Norway Geosatellite Intelligence Center

on:
  issues:
    types: [opened, reopened]

permissions:
  contents: read
  issues: write
  actions: read

concurrency:
  group: intel-metno-geosatellite-global
  cancel-in-progress: false

jobs:
  execute-metno-geosatellite:
    if: >-
      github.actor == github.repository_owner &&
      startsWith(github.event.issue.title, '[intel-metno-geosatellite]')
    runs-on: ubuntu-24.04
    timeout-minutes: 12
    env:
      GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      ISSUE_NUMBER: ${{ github.event.issue.number }}
    steps:
      - name: Checkout pinned source
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
        with:
          persist-credentials: false

      - name: Set up isolated Python
        uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: |
            api-center/requirements.txt
            api-center/metno-geosatellite/requirements.txt

      - name: Install pinned dependencies
        run: |
          python -m pip install --disable-pip-version-check --no-input \
            -r api-center/requirements.txt \
            -r api-center/metno-geosatellite/requirements.txt
          python -m pip check

      - name: Compile provider
        run: python -m py_compile api-center/managed_provider_runtime.py api-center/metno-geosatellite/metno_geosatellite_task.py

      - name: Parse and authorize MET Norway Geosatellite ticket
        id: prepare
        continue-on-error: true
        run: python api-center/metno-geosatellite/metno_geosatellite_task.py prepare --event-path "$GITHUB_EVENT_PATH" --output-dir metno-geosatellite-artifacts

      - name: Comment accepted ticket
        if: steps.prepare.outputs.accepted == 'true'
        run: |
          python api-center/metno-geosatellite/metno_geosatellite_task.py render --output-dir metno-geosatellite-artifacts --phase accepted > metno-geosatellite-comment.md
          gh api --method POST "repos/${GITHUB_REPOSITORY}/issues/${ISSUE_NUMBER}/comments" -F body=@metno-geosatellite-comment.md

      - name: Comment rejected ticket
        if: steps.prepare.outputs.accepted != 'true'
        run: |
          python api-center/metno-geosatellite/metno_geosatellite_task.py render --output-dir metno-geosatellite-artifacts --phase rejected > metno-geosatellite-comment.md
          gh api --method POST "repos/${GITHUB_REPOSITORY}/issues/${ISSUE_NUMBER}/comments" -F body=@metno-geosatellite-comment.md

      - name: Execute one bounded Geosatellite request
        id: execute
        if: steps.prepare.outputs.accepted == 'true'
        continue-on-error: true
        run: python api-center/metno-geosatellite/metno_geosatellite_task.py execute --ticket metno-geosatellite-artifacts/ticket.json --output-dir metno-geosatellite-artifacts

      - name: Upload MET Norway Geosatellite evidence
        id: upload
        if: always()
        continue-on-error: true
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: metno-geosatellite-ticket-${{ github.event.issue.number }}-${{ github.run_id }}
          path: metno-geosatellite-artifacts/
          if-no-files-found: error
          retention-days: 30

      - name: Publish verified result or structured failure
        if: always() && steps.prepare.outputs.accepted == 'true'
        env:
          ARTIFACT_URL: ${{ steps.upload.outputs.artifact-url }}
        run: |
          python api-center/metno-geosatellite/metno_geosatellite_task.py render --output-dir metno-geosatellite-artifacts --phase completed --artifact-url "$ARTIFACT_URL" > metno-geosatellite-comment.md
          gh api --method POST "repos/${GITHUB_REPOSITORY}/issues/${ISSUE_NUMBER}/comments" -F body=@metno-geosatellite-comment.md

      - name: Mark rejected or failed execution
        if: >-
          always() &&
          (steps.prepare.outputs.accepted != 'true' ||
           steps.execute.outputs.status != 'INTEL_METNO_GEOSATELLITE_COMPLETED' ||
           steps.upload.outcome != 'success')
        run: exit 1
