# 计算中心与情报中心能力归属

本文件是 `CENTER_CAPABILITY_OWNERSHIP.json` 的人工可读说明。跨中心任务只由 GPTs 中继，两个中心不得直接调用。

## 情报中心

情报中心负责与外部公开来源交互并形成可追溯证据：

- 搜索、网页、浏览器、API、公开数据集和信息源采集；
- 鉴权、限流、域名白名单、只读请求和失败回执；
- HTML、PDF、OCR、表格和结构化文档解析；
- 来源登记、快照、哈希、版本、谱系和证据包；
- 原始记录规范化、字段映射、去重准备和数据最小化；
- STIX/TAXII 等公开威胁情报交换及归档。

情报中心不负责统计推断、模型求解、仿真、优化、因果判断或最终研判。

## 计算中心

计算中心只处理票据中已经提供的结构化数据，负责可复算、受约束、断网运行的算法和模型：

- 统计、概率、计量经济、因果估计与贝叶斯推断；
- 蒙特卡洛、敏感性、不确定性、预测、校准和结果反馈；
- 优化、资源配置、运筹、系统动力学、离散事件和 Agent 仿真；
- 博弈、谈判、竞争、战略沙盘、政策反事实和金融数学；
- 图指标、关联穿透、约束验证、实体相似度和异常评分；
- VV&A、冻结基准、跨模型一致性和正式使用资格判断；
- 仅限离线、通用、非目标化的状态估计。

计算中心禁止外部取数、网页浏览、实时数据流、任意代码、动态依赖、个人识别、目标指定、武器集成、自动控制、自动交易和收益保证。

## 同名工具的双重角色

同一个 Python 包可以在两个仓库出现，但职责必须不同：

- RDFLib：情报中心构造来源与证据记录；计算中心执行图查询、结构约束和关联计算。
- RapidFuzz / datasketch：情报中心生成规范化候选块；计算中心计算相似度和碰撞分数。
- pandas / Polars：情报中心整理采集记录；计算中心分析已经移交的表格。

## 安全边界

两个中心均禁止秘密窃取、违法监控、个人或军事目标实时跟踪、影响行动、恶意网络攻击、武器效应与自主交战、自动执法、自动交易和保证盈利。
